# Leitura das notas 
notas = []
for i in range(3):
    nota = float(input(f"Digite a nota {i+1}: "))
    notas.append(nota)

# Cálculo da média
media = sum(notas) / len(notas)

# Exibição da média
print(f"A média das notas é: {media:.2f}")
